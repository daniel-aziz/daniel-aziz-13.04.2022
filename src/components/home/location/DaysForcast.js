import { DayCard } from "./DayCard";


export const DaysForcast = ({ forcast }) => {
    return (
        <>
            {/*forcast.map((item, index) => <DayCard key={index} item={item} />)*/}
        </>
    );
} 