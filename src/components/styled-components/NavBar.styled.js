import styled from "styled-components"


export const NavBarStyled = styled.nav`
    li {
        display: inline-flex;
        list-style: none;
        margin-right: 10px;
        font-size: 2rem;
    }

    li:hover {
        transform: scale(0.95);
    }

`